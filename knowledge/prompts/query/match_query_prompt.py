"""文言文学习流程相关提示词模板

定义LLM调用所需的提示词模板，用于文言文的理解、翻译、词语解释等任务。
"""

# 文言文词语解释提示词模板
CLASSICAL_WORD_EXTRACT_TEMPLATE = """
历史会话：
{history_text}

当前用户问题：{query}

请根据历史会话和当前问题，提取用户询问的文言文词语或短语（classical_words）。
1. 如果用户明确提到了某个文言文词语，请提取出来。可能有一个或多个，但不能重复。
2. 如果用户使用了代词（如“这个词”、“它”），请结合历史会话指代消解，确定词语。
3. 如果无法确定词语，classical_words 返回空列表。
4. 请重新改写用户的问题（rewritten_query），使其成为包含该词语的独立完整问题。

请直接返回JSON格式结果，格式如下：
{{
    "classical_words": ["之", "乎者", "也"],
    "rewritten_query": "请问“之”字在文言文中是什么意思？"
}}
"""

# 假设性文言文段落生成提示词
CLASSICAL_HYDE_PROMPT_TEMPLATE = """
请模拟一段文言文原文及其注释，能够回答以下用户问题。

相关词语/主题：
{word_hint}

用户问题：
{rewritten_query}

要求：
1. 以古文原文为主，适当附上现代汉语注释或翻译
2. 文言文部分应符合古代汉语语法和用词习惯，注释部分用现代汉语
3. 包含具体的句子结构、虚词用法、实词含义等
4. 不要出现“假设”、“可能”等不确定用语
5. 使用中文，300-350字，注释清晰
"""

# 实体类型说明（文言文中常见实体类型）
# 【文言文实体类型】
# 虚词、实词、句式、典故、作者、作品、篇目、注释

# LLM实体名提取提示词模版（用于文言文知识图谱）
CLASSICAL_ENTITY_EXTRACT_SYSTEM_PROMPT = """
你是一个文言文知识图谱问答系统的“实体识别”领域专家。
请从用户问题中抽取用于查询图数据库(Neo4j)的实体名称。

【允许的实体类型】
虚词、实词、句式、典故、作者、作品、篇目、注释、修辞手法、语法现象

【约束】
1) 优先抽取上述类型的名词短语（如“之乎者也”、“使动用法”、“《论语》”、“孔子”等）
2) 每个实体名称不超过 {MAX_ENTITY_NAME_LENGTH} 个字符，超过请截取核心部分，实体名不要重复
3) 不要输出完整句子，也不要错误地把现代汉语词句识别为实体，只输出实体关键词
4) 输出必须是严格 JSON，只含一个字段 entities（字符串数组）
5) 请严格遵循提示词的要求，如果违背，你将受到严重的惩罚，甚至导致系统的崩溃。

【输出示例】
{{"entities": ["之", "使动用法", "《论语》", "孔子"]}}
"""

# 回答生成提示词模板
ANSWER_PROMPT = """你是一个智能助手，请根据参考内容回答用户的问题。
要求：
尽量基于【参考内容】和【图谱关系描述】 作答，不要编造不存在的事实。

【参考内容】
{context}

【历史对话】
{history}

【相关商品/实体】
{entity_names}

【图谱关系描述】
{graph_relation_description}

【用户问题】
{question}

请回答："""